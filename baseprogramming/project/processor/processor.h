#pragma once
#include "vehicle.h"

namespace processor {
    class processor {
        public:
            float process(vehicle_space::vehicle &ovehicle, int distance);
        protected:
        private:    
    };
}